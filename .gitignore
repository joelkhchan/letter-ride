node_modules/
.DS_Store
android/
.gradle/
.superpowers/
www/
.playwright-mcp/
logs/

# eval-harness build scratch
.sdd/

# session scratch (Playwright screenshots + standalone preview pages at repo root)
/*.png
/*-preview.html
# downloaded build artifact (grab from CI; never commit)
*.apk
# canvas dataURL scratch (icon rendering)
/icon-*.txt
